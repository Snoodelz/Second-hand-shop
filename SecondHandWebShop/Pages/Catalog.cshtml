@page
@model CatalogModel
@{
    ViewData["Title"] = "Katalog";
}

<div class="container">

    <div class="row">

        <div class="col-lg-3">

            <img src="img/logo.png">
            <div class="list-group">
                <a href="Catalog?Category=all" class="list-group-item">Alla produkter</a>
                <a href="Catalog?Category=merchandise" class="list-group-item">Merchandise</a>
                <a href="Catalog?Category=hat" class="list-group-item">Hattar</a>
                <a href="Catalog?Category=jacket" class="list-group-item">Jackor</a>
                <a href="Catalog?Category=jewelry" class="list-group-item">Smycken</a>
                <a href="Catalog?Category=shirt" class="list-group-item">Tröjor</a>
                <a href="Catalog?Category=trouser" class="list-group-item">Byxor</a>
                <a href="Catalog?Category=shoe" class="list-group-item">Skor</a>
                <a href="Catalog?Category=discount" class="list-group-item">Rea</a>
            </div>

        </div>
        <!-- /.col-lg-3 -->

        <div class="col-lg-9">

            <div class="row">
                @switch (Model.Category)
                {
                    case "all":
            @foreach (var item in Model.AllClothing)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>} break;
        case "merchandise":
            @foreach (var item in Model.Merchandise)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>} break;
        case "hat":
            @foreach (var item in Model.AllHats)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>} break;
        case "jacket":
            @foreach (var item in Model.AllJackets)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>} break;
        case "jewelry":
            @foreach (var item in Model.AllJewelries)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>}break;
        case "shirt":
            @foreach (var item in Model.AllShirts)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>}break;
        case "trouser":
            @foreach (var item in Model.AllTrousers)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>}break;
        case "shoe":
            @foreach (var item in Model.AllShoes)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>}break;
        case "discount":
            @foreach (var item in Model.ProductsOnDiscount)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>}break;

        default:
            @foreach (var item in Model.AllClothing)
            {
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="@item.ImageUrl" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">@item.Name</a>
                        </h4>
                        @if (item.Discount > 0)
                        {
                            item.DiscountedPrice = item.Price - (item.Price * item.Discount) / 100;
                            <h5><s>@Math.Round(item.Price) kr</s> @Math.Round(item.DiscountedPrice) kr </h5>
                            <p style="color:palevioletred">@Math.Round(item.Discount)% OFF</p>
                        }
                        else
                        {
                            <h5>@item.Price kr</h5>
                        }
                        <p class="card-text">@item.Description</p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">@item.Brand</small>
                    </div>
                </div>
            </div>}break;
                        }


            </div>
            <!-- /.row -->

        </div>
        <!-- /.col-lg-9 -->

    </div>
    <!-- /.row -->

</div>
<!-- /.container -->